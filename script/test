#!/bin/bash
set -e

./script/build

# If gotest exists, colorize the output.
if [ -f $GOBIN/gotest ]
then
     gotest -v github.com/leosunmo/vulcanizer/...
else
    go test -v github.com/leosunmo/vulcanizer/...
fi

golangci-lint -v run
